<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/league_title_view_root"
    android:layout_width="match_parent"
    android:layout_height="48dp">

    <TextView
        android:id="@+id/tv_league_title"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="20dp"
        android:gravity="center_vertical"
        android:textSize="@dimen/material_text_title"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/iv_league_logo"
        app:layout_constraintEnd_toStartOf="@+id/iv_league_logo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/iv_league_logo"
        tools:text="لیگ برتر خلیج فارس" />

    <ImageView
        android:id="@+id/iv_league_logo"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginEnd="16dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/iv_grip"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_grip"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="16dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_grip_24" />

</androidx.constraintlayout.widget.ConstraintLayout>